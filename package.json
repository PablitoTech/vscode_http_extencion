{
  "name": "spring-http-generator",
  "displayName": "Spring HTTP Generator (0.0.1 Beta)",
  "description": "🚀 Generación automática de archivos .http para Spring Boot. Acelera tus pruebas de API con un solo click.",
  "version": "0.0.1",
  "publisher": "PablitoTech",
  "icon": "resources/logo.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/spring-http-generator"
  },
  "categories": [
    "Other",
    "Snippets"
  ],
  "keywords": [
    "spring",
    "spring boot",
    "rest",
    "http",
    "rest client",
    "api testing"
  ],
  "activationEvents": [
    "*",
    "onLanguage:java"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "spring-http-generator.generateHttpFile",
        "title": "Generar archivo HTTP (Todos los controladores)"
      },
      {
        "command": "spring-http-generator.generateHttpFromController",
        "title": "Generar archivo .http desde Controlador"
      },
      {
        "command": "spring-http-generator.generateHttpFromMethod",
        "title": "Generar solicitud HTTP desde Método"
      },
      {
        "command": "spring-http-generator.runDiagnostics",
        "title": "Ejecutar diagnósticos de la extensión"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "when": "resourceLangId == java",
          "command": "spring-http-generator.generateHttpFromController",
          "group": "spring-http-generator"
        }
      ],
      "editor/context": [
        {
          "when": "resourceLangId == java",
          "command": "spring-http-generator.generateHttpFromController",
          "group": "spring-http-generator@1"
        },
        {
          "when": "resourceLangId == java && editorHasSelection",
          "command": "spring-http-generator.generateHttpFromMethod",
          "group": "spring-http-generator@2"
        }
      ],
      "commandPalette": [
        {
          "command": "spring-http-generator.generateHttpFile",
          "when": "workspaceFolderCount > 0"
        },
        {
          "command": "spring-http-generator.generateHttpFromController",
          "when": "resourceLangId == java"
        },
        {
          "command": "spring-http-generator.generateHttpFromMethod",
          "when": "resourceLangId == java && editorHasSelection"
        }
      ]
    },
    "configuration": {
      "title": "Spring HTTP Generator",
      "properties": {
        "spring-http-generator.baseUrl": {
          "type": "string",
          "default": "http://localhost:8080",
          "description": "URL base para las solicitudes HTTP generadas."
        },
        "spring-http-generator.outputDirectory": {
          "type": "string",
          "default": "http-requests",
          "description": "Directorio donde se guardarán los archivos .http generados."
        },
        "spring-http-generator.includeAuthHeader": {
          "type": "boolean",
          "default": true,
          "description": "Incluir automáticamente la cabecera 'Authorization' cuando se detecten anotaciones de seguridad."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "test": "jest",
    "package": "vsce package",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.6",
    "@types/vscode": "^1.85.0",
    "@vscode/test-electron": "^2.3.8",
    "esbuild": "^0.19.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3",
    "vscode-test": "^1.6.1"
  },
  "dependencies": {}
}